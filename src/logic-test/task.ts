import { FilterDefinition, FilterFunction } from './definition';

/**
* TASK
* 
* You are to create a function that takes a "filter defintion" parameter 
* which you can find in the defintion.ts file. The function you create, should return a new
* filter function that can be used to filter an array. 
*
* You may create other functions if you need to, but should not change anything in the test cases.
* If are not able to complete the typings, you may use the 'any' type wherever necessary.
*/

// Create this function so that the test cases in test.ts are executed directly. Replace the
// `any` type declarations with your own declarations.
export const createFilterFromDefinition = (defintion) => {};
